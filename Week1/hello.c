#include <stdio.h>
#include <cs50.h>

int main(void)
{
    // Print and Strings

    /* string answer = get_string("What's your name? ");
     printf("Hello, %s\n", answer);

     // Intergers and basic if / else statements

     int x = 15;
     int y = 15;

     if (x < y)
     {
        printf("x is less than y\n");
     }
     else if (x > y)
     {
        printf("y is less than x\n");
     }
     else if (x == y)
     {
        printf("x is equal than y\n");
     }

     */

     // Variables

     int counter = 0;

     counter = counter + 1; // counter++ is better, less is more.

     counter = counter - 1; // counter-- is better, less is more.ls
}
